{
  "name": "site-manager",
  "private": true,
  "scripts": {
    "build": "esbuild entry.jsx --bundle --format=esm --minify --jsx=automatic --outfile=bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "lucide-react": "^0.436.0"
  }
}
