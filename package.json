{
  "name": "site-manager",
  "private": true,
  "scripts": {
    "build": "esbuild entry.jsx --bundle --format=esm --minify --jsx=automatic --outfile=bundle.js && node scripts/stamp.mjs",
    "test:rules": "firebase emulators:exec --only firestore \"node rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "esbuild": "^0.23.0",
    "firebase": "^12.18.0"
  },
  "dependencies": {
    "lucide-react": "^0.436.0",
    "qrcode": "^1.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
